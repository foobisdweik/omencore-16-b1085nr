<UserControl x:Class="OmenCore.Controls.HardwareMonitoringDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OmenCore.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource DarkBackgroundBrush}">

    <UserControl.Resources>
        <Style x:Key="MetricCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15"/>
        </Style>

        <Style x:Key="MetricTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="MetricUnitStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ChartButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,10">
            <StackPanel Margin="15">
                <TextBlock Text="Hardware Monitoring Dashboard" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>
                <TextBlock Text="Real-time monitoring of power consumption, battery health, and system performance with historical trends and alerts." FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,0,10">

                <!-- Current Metrics Row -->
                <TextBlock Text="Current Metrics" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>

                <UniformGrid Columns="4" Margin="0,0,0,20">
                    <!-- Power Consumption -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Power Consumption" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="PowerConsumptionValue" Text="0.0" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="Watts" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock x:Name="PowerConsumptionTrend" Text="↗ +2.1W" FontSize="11" Foreground="{DynamicResource WarningBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Battery Health -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Battery Health" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="BatteryHealthValue" Text="100" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="%" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock x:Name="BatteryHealthStatus" Text="Excellent" FontSize="11" Foreground="{DynamicResource SuccessBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- CPU Temperature -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="CPU Temperature" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="CpuTempValue" Text="45" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="°C" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock x:Name="CpuTempStatus" Text="Normal" FontSize="11" Foreground="{DynamicResource SuccessBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- GPU Temperature -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="GPU Temperature" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="GpuTempValue" Text="52" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="°C" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock x:Name="GpuTempStatus" Text="Normal" FontSize="11" Foreground="{DynamicResource SuccessBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- System Load & Clock Speeds Row -->
                <TextBlock Text="System Activity" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>
                
                <UniformGrid Columns="4" Margin="0,0,0,20">
                    <!-- CPU Load -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="CPU Load" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="CpuLoadValue" Text="0" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="%" Style="{StaticResource MetricUnitStyle}"/>
                            <ProgressBar x:Name="CpuLoadBar" Height="4" Background="{DynamicResource DarkBackgroundBrush}" Foreground="{DynamicResource SuccessBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- GPU Load -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="GPU Load" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="GpuLoadValue" Text="0" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="%" Style="{StaticResource MetricUnitStyle}"/>
                            <ProgressBar x:Name="GpuLoadBar" Height="4" Background="{DynamicResource DarkBackgroundBrush}" Foreground="{DynamicResource WarningBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- CPU Clock Speed -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="CPU Clock" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="CpuClockValue" Text="0" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="MHz" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock Text="Average core clock" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- RAM Usage -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="RAM Usage" Style="{StaticResource MetricTitleStyle}"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="RamUsageValue" Text="0.0" Style="{StaticResource MetricValueStyle}"/>
                                <TextBlock Text=" GB" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="RamUsagePercent" Text="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock Text="% used" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Charts Section -->
                <TextBlock Text="Historical Trends" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>

                <!-- Auto-display Charts Grid -->
                <UniformGrid x:Name="ChartsGrid" Columns="2" Rows="2" Margin="0,0,0,10">
                    <!-- Power Chart -->
                    <Border x:Name="PowerChartBorder" Style="{StaticResource MetricCardStyle}" MinHeight="200">
                        <StackPanel>
                            <TextBlock Text="⚡ Power Consumption" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
                            <Canvas x:Name="PowerChartCanvas" Height="150" Background="{DynamicResource DarkBackgroundBrush}"/>
                            <TextBlock x:Name="PowerChartStats" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Temperature Chart -->
                    <Border x:Name="TempChartBorder" Style="{StaticResource MetricCardStyle}" MinHeight="200">
                        <StackPanel>
                            <TextBlock Text="🌡️ Temperature" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
                            <Canvas x:Name="TempChartCanvas" Height="150" Background="{DynamicResource DarkBackgroundBrush}"/>
                            <TextBlock x:Name="TempChartStats" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Battery Chart -->
                    <Border x:Name="BatteryChartBorder" Style="{StaticResource MetricCardStyle}" MinHeight="200">
                        <StackPanel>
                            <TextBlock Text="🔋 Battery Health" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
                            <Canvas x:Name="BatteryChartCanvas" Height="150" Background="{DynamicResource DarkBackgroundBrush}"/>
                            <TextBlock x:Name="BatteryChartStats" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Fan Chart -->
                    <Border x:Name="FanChartBorder" Style="{StaticResource MetricCardStyle}" MinHeight="200">
                        <StackPanel>
                            <TextBlock Text="🌀 Fan Speeds" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
                            <Canvas x:Name="FanChartCanvas" Height="150" Background="{DynamicResource DarkBackgroundBrush}"/>
                            <TextBlock x:Name="FanChartStats" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Chart Controls (for export/refresh) -->
                <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Margin="15" HorizontalAlignment="Center">
                        <Button x:Name="RefreshDataButton" Content="🔄 Refresh Charts" Style="{StaticResource ChartButtonStyle}" Click="RefreshDataButton_Click" Margin="5,0"/>
                        <Button x:Name="ExportDataButton" Content="💾 Export Data" Style="{StaticResource ChartButtonStyle}" Click="ExportDataButton_Click" Margin="5,0"/>
                    </StackPanel>
                </Border>

                <!-- Alerts Section -->
                <TextBlock Text="System Alerts" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>

                <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,10">
                    <StackPanel Margin="15">
                        <TextBlock Text="Active Alerts" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>
                        <ListBox x:Name="AlertsListBox" Height="120" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource WarningBackgroundBrush}" BorderBrush="{DynamicResource WarningBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,2">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Icon}" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="{DynamicResource WarningBrush}"/>
                                                <TextBlock Text="{Binding Message}" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Timestamp}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock x:Name="NoAlertsText" Text="No active alerts" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" Margin="0,20"/>
                    </StackPanel>
                </Border>

                <!-- Efficiency Metrics -->
                <TextBlock Text="Efficiency Metrics" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>

                <UniformGrid Columns="3" Margin="0,0,0,20">
                    <!-- Power Efficiency -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Power Efficiency" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="PowerEfficiencyValue" Text="85.2" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="%" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock x:Name="PowerEfficiencyRating" Text="Good" FontSize="11" Foreground="{DynamicResource SuccessBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Thermal Status -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Thermal Status" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="ThermalStatusValue" Text="Optimal" Style="{StaticResource MetricValueStyle}" FontSize="20"/>
                            <TextBlock Text="condition" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock x:Name="ThermalEfficiency" Text="Fan: 68% efficient" FontSize="10" Foreground="{DynamicResource InfoBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Battery Health & Cycles -->
                    <Border Style="{StaticResource MetricCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Battery Status" Style="{StaticResource MetricTitleStyle}"/>
                            <TextBlock x:Name="BatteryCyclesValue" Text="127" Style="{StaticResource MetricValueStyle}"/>
                            <TextBlock Text="cycles" Style="{StaticResource MetricUnitStyle}"/>
                            <TextBlock x:Name="BatteryLifeEstimate" Text="3.0 years remaining" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>